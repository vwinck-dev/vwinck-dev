$ whoami

  V. Winck — fullstack devsecops, Brazil
  PHP · Laravel · clean architecture · software engineering undergrad

$ cat ~/now

  studying    devsecops — secure sdlc, threat modeling
  building    boardflow-core — the chess core below

$ ls ~/projects

  boardflow/           modular chess ecosystem for PHP — framework-
                       agnostic core: move validation, game state,
                       FEN/PGN, pluggable engines
  draftcircle/         social platform where authors write, publish
                       and discuss books together
  laravel-translator/  publish, manage and extend Laravel translations
                       with a single artisan command
  edgelink/            edge microservice for ESP32 sensor networks,
                       RSA encryption at the edge — wip
  chimera/             modular discord bot — moderation, music and
                       automation (discord.js)
  xlsx-to-insert-into/ turns spreadsheets into ready-to-run SQL
                       INSERT statements
  vwinck-dev/          this profile — the terminal you are reading is
                       generated from a text file by a python package

$ composer global show | grep -E 'boardflow|vwinck'

  boardflow/boardflow-core        chess core, zero framework deps
  vwinck-dev/laravel-translator   i18n utilities for Laravel

$ cat ~/stack

  languages   php (5.x–8.x) · javascript · python · c/c++
  backend     laravel 9–12 · rest apis · queues (horizon) · sanctum
  frontend    vue · inertia · tailwind · vite · blade
  data        mysql/mariadb · redis · sqlite
  security    threat modeling · secure code review ·
              dependency scanning · owasp top 10
  quality     pest · phpunit · phpstan/larastan · pint · rector · tdd
  devops      docker · github actions · gitlab ci · okd/openshift

$ git log --author="V. Winck" --oneline

  18428f3  fix(security): path traversal, phpinfo exposure, password hash
  02aae99  fix(security): sqli, xss and csrf in a legacy php 5.x portal
  4309ee1  fix(auth): close an auth bypass and a broken ckfinder base url
  d0278dd  refactor(core): domain enums, zero magic literals policy
  6036c44  feat(filament): finish the laravel nova to filament 5 migration
  81d25a9  feat(a11y): wcag, high contrast and mobile on a public portal
  88c3ad4  fix(okd): securityContext for openshift PodSecurity restricted
  7a8dc40  fix(pdf): resolve gateway timeout on massive pdf generation
  5139c19  feat(cache): resource-based caching and invalidation system
  17fad1b  feat(auth): single sign-on for a municipal education program
  7bc3092  feat(console): artisan lang commands for laravel-translator
  40bb2f1  feat(presence): discord rich presence for a snes emulator
  d90a73e  feat(demo): interactive dos attack demo with live mitigation
  62a327a  refactor: drop duplicated methods across controllers and models

$ cat ~/.contact

  packagist   packagist.org/users/vwinck-dev
  discord     @vwinckdev
  email       vwincknascimento@gmail.com

$ exit
